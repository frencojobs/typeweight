name: typeweight
description: A more readable font weights for flutter/material.
version: 0.0.1
homepage: https://github.com/frencojobs/typeweight

environment:
  sdk: ">=2.5.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.6.0
  flutter_test:
    sdk: flutter
