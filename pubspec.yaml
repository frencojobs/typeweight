name: typeweight
description: Flutter plugin to help you to use more readable/clear font weight definitions.
version: 2.0.0
homepage: https://github.com/frencojobs/typeweight

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.0
  test: ^1.6.0
